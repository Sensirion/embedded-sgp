version: 2
jobs:
  build:
    docker:
        - image: gcc:5.4.0 
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
      - run:
          name: buildIt
          command: make release

